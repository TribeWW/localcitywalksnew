/**
 * Field values for the checkout contact form (summary pay step).
 */

export interface CheckoutContactFieldsValues {
  firstName: string;
  lastName: string;
  email: string;
  phone: string;
  comments: string;
}
